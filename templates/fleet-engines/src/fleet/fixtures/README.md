# Fleet fixtures — real workflows, verbatim

Verbatim copies of live workflow files from the bamr87 fleet, used by the Fleet Ops engine tests (`facts.test.ts`, `audit.test.ts`) as ground truth. Prefix = source repo: `zm--` zer0-mistakes · `lh--` lifehacker.dev · `ij--` it-journey · `cv--` cv-builder-pro · `hub--` bamr87/bamr87.

Do not edit these by hand — refresh by re-copying from the source repo when a test needs newer behaviour, and say so in the commit message. Tests import them with `?raw`.
