# <Feature title> — evidence (<PR #N | issue #N | scenario id>)

> **Feature:** `<FEATURE-ID>` in `features/features.yml` · **Scenario:** `verify/scenarios/<slug>.yml` · **Generated by:** `node verify/runner.mjs --only <slug>` against `<base url>` on `<YYYY-MM-DD>` · **Verified by:** agent | human | ci (`<run url>`)

One paragraph: what the user can do, what changed, and what these images prove. If this is a fix, say what the bug looked like (the BEFORE) and what the fix looks like (the AFTER). Numbers beat adjectives: "page overflow 653px → 0 across 8 widths" is evidence; "looks better" is not.

## What each image shows

| Image | Viewport | What to look at |
| --- | --- | --- |
| `desktop-home.png` | 1280×800 | … |
| `mobile-home.png` | 390×844 | … |

## Metrics

Raw data: [`report.json`](report.json) — every step's status, console errors, timings, and `eval` values.

## Not covered

What this evidence does NOT prove (other browsers, real data, auth'd paths), so the next reader knows where the edge is.
