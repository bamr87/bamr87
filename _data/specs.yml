# =============================================================================
# _data/specs.yml — the Universal Project Standard, machine-readable
# =============================================================================
# GENERATED by tools/gen-specs-data.py from the requirement tables in specs/*.md.
# Do not hand-edit: change the spec table, then regenerate. The drift gate's
# `--check` mode fails when this file is stale.
#
# Per requirement: id, area, level (MUST|SHOULD|MAY), applies (stack kinds from
# specs/STACKS.md; `all` binds every kind, notes carry exceptions), the prose
# requirement, what satisfies it, and the kit/tool that seeds it (`—` = gap).
# Consumed by: the UPS audit (dash audit --spec, roadmap) and the dash site.
# =============================================================================

version: "1.0"
status: draft
kinds: [all, site, app, api, lib, cli, ext, content, fork]
areas:
  - id: REPO
    file: specs/REPOSITORY.md
    title: "Repository layout and required files"
  - id: AGENT
    file: specs/AGENT-CONTEXT.md
    title: "Agent context"
  - id: QA
    file: specs/QUALITY.md
    title: "Quality gates"
  - id: FE
    file: specs/FRONTEND.md
    title: "Frontend: design system, core components, UX standards"
  - id: FB
    file: specs/FEEDBACK.md
    title: "The universal feedback component"
  - id: BE
    file: specs/BACKEND.md
    title: "HTTP API conventions and the client contract"
  - id: OPS
    file: specs/OPERATIONS.md
    title: "Configuration, observability, security, data"
requirements:
  - id: UPS-REPO-01
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "Root holds only: docs-of-record (`README.md`, `CLAUDE.md`/`AGENTS.md`, `LICENSE`, `CHANGELOG.md`, `SECURITY.md`, `CONTRIBUTING.md`, `SCHEMA.md`), tool manifests, dotfiles, and top-level directories from the stack profile. Source never sits loose at the root."
    satisfied_by: "`SCHEMA.md` Structure table lists every root entry"
    seed: "`templates/schema/`"
    gap: false
  - id: UPS-REPO-02
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "Source lives under the stack's canonical source dir (`src/`, `app/`, `pages/`, `lib/`, or the framework's own convention — see [STACKS.md](STACKS.md)); tests under `tests/` (Python/Node/Bash) or the framework's own (`test/` for Ruby gems and Playwright suites, `cypress/` retired in favour of Playwright). New repos use `tests/`; existing `test/` dirs are grandfathered and recorded as a deviation."
    satisfied_by: "dir present, referenced from `SCHEMA.md`"
    seed: "`scripts/project-init.sh`"
    gap: false
  - id: UPS-REPO-03
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "`docs/` holds human docs beyond the README (`UPPERCASE-TOPIC.md` for operator docs, lowercase for guides). A repo with a docs *site* declares its generator (`mkdocs.yml` or the Jekyll site itself); a bare `docs/` with no index is a warning."
    satisfied_by: "`docs/README.md` index exists"
    seed: "—"
    gap: true
  - id: UPS-REPO-04
    area: REPO
    section: "Layout"
    level: SHOULD
    applies: [all]
    requirement: "`tools/` holds the repo's own operator scripts (kebab-case, `#!/usr/bin/env bash`, `set -euo pipefail`, shellcheck-clean) and nothing that ships to users."
    satisfied_by: "`tools/` + `tools/README.md`"
    seed: "—"
    gap: true
  - id: UPS-REPO-05
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "Multi-surface repos (backend + frontend, extension + CLI) keep each surface in its own top-level dir with its own manifest and README; the root README links them."
    satisfied_by: "e.g. `backend/`, `frontend/` each with `README.md`"
    seed: "—"
    gap: true
  - id: UPS-REPO-06
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "Every directory with more than trivial content has a `README.md` (README-First/README-Last) and, in a schema-adopted repo, a `SCHEMA.md`."
    satisfied_by: "file present"
    seed: "`templates/schema/`, `.github/templates/README.template.md`"
    gap: false
  - id: UPS-REPO-07
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "No committed build output, `node_modules/`, lockfiles, `.env`, or secrets. `.gitignore` carries the fleet lockfile block."
    satisfied_by: "`.gitignore` contains the `unpin-deps.sh` block; `git ls-files` clean"
    seed: "`tools/unpin-deps.sh`"
    gap: false
  - id: UPS-REPO-08
    area: REPO
    section: "Layout"
    level: MUST
    applies: [all]
    requirement: "Exactly one checkout per upstream in the fleet. Duplicate directories tracking the same remote (2026-09: `edgar-data-parse`≡`fredgar-ai`, `vscode-front-matter`≡`zer0-cms`) are a registry defect, not two projects."
    satisfied_by: "registry + `.gitmodules` have one path per `repo_url`"
    seed: "`dash reconcile`"
    gap: false
  - id: UPS-REPO-10
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    requirement: "`README.md` opens with `# <name>`, a one-sentence tagline in a blockquote, then a badge row (CI, release, license), then sections in this order: **What it is · Quick start · Usage · Development · Testing · Deployment (if any) · Contributing · License**. Content repos may collapse Usage/Development/Testing into one **Working with this repo** section."
    satisfied_by: "headings present in order"
    seed: "`.github/templates/README.template.md` (to be extended)"
    gap: false
  - id: UPS-REPO-11
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    requirement: "Quick start is copy-pasteable: one install command, one run command, one test command, each in a fenced block, and they match `CLAUDE.md` **Stack & commands** exactly."
    satisfied_by: "commands identical in both files"
    seed: "—"
    gap: true
  - id: UPS-REPO-12
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`LICENSE` is present and its SPDX id is recorded in the registry `license:` field. Default for bamr87-owned repos is **MIT**; content repos MAY use CC-BY-4.0 and say so in the README."
    satisfied_by: "`LICENSE` + registry field"
    seed: "`templates/license/` (gap — see CONFORMANCE)"
    gap: true
  - id: UPS-REPO-13
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all, fork]
    applies_notes: ["all except content"]
    requirement: "`CHANGELOG.md` exists and is release-please managed (Keep-a-Changelog headings, newest first). Hand-edits go only above the first release heading."
    satisfied_by: "file + `release-please-config.json`"
    seed: "`tools/adopt-release.sh`"
    gap: false
  - id: UPS-REPO-14
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [app, api, ext, lib, cli]
    requirement: "`SECURITY.md` states the supported versions, how to report privately (GitHub private vulnerability reporting), and the response window (fleet default: acknowledge within 7 days)."
    satisfied_by: "file with those three headings"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-REPO-15
    area: REPO
    section: "Required files — content contracts"
    level: SHOULD
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`CONTRIBUTING.md` covers: branch naming, Conventional Commits, how to run the gates locally, the PR template, and the submodule rule (commit here first, hub bumps the pointer). Repos may instead link the hub's `CONTRIBUTING.md` in one line."
    satisfied_by: "file or README link"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-REPO-16
    area: REPO
    section: "Required files — content contracts"
    level: SHOULD
    applies: [all]
    requirement: "`CODEOWNERS` exists (soft template: `* @bamr87`) so the issue pipeline's owner routing has a default."
    satisfied_by: "`.github/CODEOWNERS`"
    seed: "`.github/templates/CODEOWNERS-soft.template`"
    gap: false
  - id: UPS-REPO-17
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    requirement: "`.editorconfig` is the hub's byte-for-byte (utf-8, lf, final newline, trim trailing; 2-space yml/json/js/ts/sh, 4-space py with `max_line_length = 120`, tab Makefile, md exempt from trim)."
    satisfied_by: "`cmp` with hub copy"
    seed: "`tools/fanout.sh --kit standardize --artifacts editorconfig`"
    gap: false
  - id: UPS-REPO-18
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    requirement: "Issue templates: `bug_report.yml`, `feature_request.yml`, `documentation.yml` plus the feedback-widget template `page_feedback.yml` (see [FEEDBACK.md](FEEDBACK.md)); each applies the type label from the fleet taxonomy so filed issues enter the issue pipeline."
    satisfied_by: "`.github/ISSUE_TEMPLATE/*.yml`"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-REPO-19
    area: REPO
    section: "Required files — content contracts"
    level: SHOULD
    applies: [all]
    requirement: "`.github/pull_request_template.md` with: summary, linked issue, checklist (gates green, README-Last done, SCHEMA.md updated)."
    satisfied_by: "file present"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-REPO-20
    area: REPO
    section: "Required files — content contracts"
    level: MUST
    applies: [all]
    requirement: "Front matter and file headers follow the house rule: shell/python tools carry the header block (File, Description, Author, Created, Last Modified, Version, Usage); Jekyll content carries `title`, `description`, `date`/`lastmod`, `tags`, `categories`, `permalink` (see [FRONTEND.md](FRONTEND.md) FE-40)."
    satisfied_by: "header present"
    seed: "`.github/copilot-instructions.md` § File Headers"
    gap: false
  - id: UPS-REPO-30
    area: REPO
    section: "Containers and local environment"
    level: SHOULD
    applies: [app, api, site]
    requirement: "A `docker-compose.yml` (or `compose.yml`) runs the whole app locally with one command; a `.devcontainer/devcontainer.json` composes it and pins the fleet VS Code extension set. Ports follow the fleet map: 4000 Jekyll, 5000 web app, 5173 Vite HMR, 8000 API/MkDocs, 5432 Postgres, 6379 Redis."
    satisfied_by: "files present, ports match"
    seed: "hub `.devcontainer/`, `docker-compose.yml` as reference"
    gap: false
  - id: UPS-REPO-31
    area: REPO
    section: "Containers and local environment"
    level: MUST
    applies: [app, api]
    requirement: "Dockerfiles are multi-stage, run as a non-root user, declare a `HEALTHCHECK` hitting the health endpoint (BE-10), and never copy `.env`."
    satisfied_by: "Dockerfile review"
    seed: "`.github/copilot-instructions.md` § Container-First"
    gap: false
  - id: UPS-REPO-32
    area: REPO
    section: "Containers and local environment"
    level: MUST
    applies: [all]
    applies_notes: ["all with env config"]
    requirement: "`.env.example` lists every variable the app reads with a comment and a safe placeholder; the app fails fast on a missing required variable (OPS-01)."
    satisfied_by: "file present, names match code"
    seed: "—"
    gap: true
  - id: UPS-REPO-33
    area: REPO
    section: "Containers and local environment"
    level: SHOULD
    applies: [all]
    requirement: "One bootstrap script (`tools/setup.sh` or the stack's `make setup`/`npm run setup`) brings a fresh clone to a passing test run; the README Quick start calls it."
    satisfied_by: "script present"
    seed: "`scripts/project-init.sh`"
    gap: false
  - id: UPS-AGENT-01
    area: AGENT
    section: "The context file"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "A root `CLAUDE.md` exists. `AGENTS.md`, when present, is the tool-neutral copy and MUST NOT contradict it; `CLAUDE.md` may be a one-line pointer to `AGENTS.md`."
    satisfied_by: "file present"
    seed: "`templates/agent-context/CLAUDE.template.md`"
    gap: false
  - id: UPS-AGENT-02
    area: AGENT
    section: "The context file"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`CLAUDE.md` carries these headings, in order: **What this repo is** (one paragraph, who it serves, what \"done\" means) · **Stack & commands** (install/run/test/lint/build as fenced commands, identical to README Quick start) · **Layout** (top-level dirs, or \"see SCHEMA.md\") · **Conventions** (commits, branches, the do-nots) · **Fleet context** (the submodule rule + link to the hub) · **Standard deviations** (UPS ids waived and why; \"none\" if none)."
    satisfied_by: "headings present; no `TODO:` scaffold markers left"
    seed: "kit scaffold + hand fill"
    gap: false
  - id: UPS-AGENT-03
    area: AGENT
    section: "The context file"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "The kit stamp `<!-- kit: agent-context vX.Y.Z -->` is preserved so `dash audit` can flag version drift and unfilled scaffolds."
    satisfied_by: "stamp present"
    seed: "kit"
    gap: false
  - id: UPS-AGENT-04
    area: AGENT
    section: "The context file"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "The do-nots are explicit: never push to the default branch, never commit lockfiles or secrets, never suppress type errors, never leave empty exception handlers, never edit generated files by hand."
    satisfied_by: "text present"
    seed: "kit"
    gap: false
  - id: UPS-AGENT-05
    area: AGENT
    section: "The context file"
    level: SHOULD
    applies: [all]
    requirement: "Where the repo has scoped rules, they live in `.github/instructions/*.instructions.md` with `applyTo` globs (Copilot format, readable by every agent), not scattered in comments."
    satisfied_by: "dir present"
    seed: "hub `.github/instructions/` as reference"
    gap: false
  - id: UPS-AGENT-06
    area: AGENT
    section: "The context file"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`.github/workflows/claude.yml` is the kit's `@claude` mention handler, OAuth-first (`CLAUDE_CODE_OAUTH_TOKEN`, `ANTHROPIC_API_KEY` fallback), with `checkout@v7`. Hand-modified copies keep the kit stamp and are excluded from `--upgrade`."
    satisfied_by: "byte-identical to kit or archive, or stamped + documented"
    seed: "`tools/fanout.sh --artifacts claude`"
    gap: false
  - id: UPS-AGENT-10
    area: AGENT
    section: "`.claude/` baseline"
    level: SHOULD
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`.claude/settings.json` exists with the schemastore `$schema` and the kit's read-only permission allowlist as its floor; repos add to it, never remove from it."
    satisfied_by: "file present, superset of kit"
    seed: "`--artifacts claude-settings`"
    gap: false
  - id: UPS-AGENT-11
    area: AGENT
    section: "`.claude/` baseline"
    level: MUST
    applies: [all]
    requirement: "Hooks, skills, commands, agents, and agent memory are **repo-local** and never fanned out by default. Two sanctioned opt-in kit artifacts exist: `claude-guardrails` (`.claude/skills/_shared/quarantine.md`) and `claude-agent-auditor`."
    satisfied_by: "kit VERSION 0.4.0 position"
    seed: "`templates/agent-context/`"
    gap: false
  - id: UPS-AGENT-12
    area: AGENT
    section: "`.claude/` baseline"
    level: MUST
    applies: [all]
    applies_notes: ["all with `.claude/hooks`"]
    requirement: "Hooks are `python3`, fail-open (exit 0 when a dependency is missing), throttled, and never write silently."
    satisfied_by: "code review"
    seed: "hub `.claude/hooks/` as reference"
    gap: false
  - id: UPS-AGENT-13
    area: AGENT
    section: "`.claude/` baseline"
    level: SHOULD
    applies: [all]
    applies_notes: ["all with `.claude/`"]
    requirement: "Frontmatter names are kebab-case; no `model:` pins unless a role demonstrably needs one; command names are namespaced to avoid collisions with hub commands (`/evolve`, `/dash-status`)."
    satisfied_by: "review"
    seed: "kit VERSION notes"
    gap: false
  - id: UPS-AGENT-14
    area: AGENT
    section: "`.claude/` baseline"
    level: MUST
    applies: [all]
    applies_notes: ["all with `.claude/agents`"]
    requirement: "Every agent cites the shared guardrails doc in one line instead of restating them (untrusted-input quarantine, honesty rule, merge discipline)."
    satisfied_by: "line present"
    seed: "`--artifacts claude-guardrails`"
    gap: false
  - id: UPS-AGENT-15
    area: AGENT
    section: "`.claude/` baseline"
    level: SHOULD
    applies: [app, api, lib]
    requirement: "`.mcp.json` declares the MCP servers the repo's agents rely on (fleet baseline: `github`, `memory`, `sequentialthinking`, `context7`), with no credentials inline."
    satisfied_by: "file present"
    seed: "hub `.mcp.json`"
    gap: false
  - id: UPS-AGENT-20
    area: AGENT
    section: "Structural contract (Pyramid Schema)"
    level: SHOULD
    applies: [all, content]
    applies_notes: ["all except fork"]
    requirement: "The repo carries a `SCHEMA.md` pyramid (one per directory, from the kit template), the vendored `tools/schema_lint.py`, the `schema-check.yml` gate, and the protocol snippet in `CLAUDE.md`. Registry `schema.status` records adoption."
    satisfied_by: "`schema_lint.py check .` passes"
    seed: "`tools/seed-schema.sh` / `schema-fanout.yml`"
    gap: false
  - id: UPS-AGENT-21
    area: AGENT
    section: "Structural contract (Pyramid Schema)"
    level: MUST
    applies: [all]
    applies_notes: ["all with SCHEMA.md"]
    requirement: "Any add/remove/rename updates the local `SCHEMA.md` in the same commit; generated entries are marked `generated`; sub-repos and vendored trees are `terminal`."
    satisfied_by: "lint clean"
    seed: "kit"
    gap: false
  - id: UPS-AGENT-22
    area: AGENT
    section: "Structural contract (Pyramid Schema)"
    level: MUST
    applies: [all]
    requirement: "Vendored hub tools (`schema_lint.py`, `unwrap-prose.py`) stay byte-identical to the hub copy; drift check (i) reports divergence."
    satisfied_by: "`cmp`"
    seed: "fan-out re-vendor"
    gap: false
  - id: UPS-AGENT-30
    area: AGENT
    section: "Agent-facing operational rules"
    level: MUST
    applies: [all]
    requirement: "Agents open PRs only — never merge, never push to a protected branch; workflows that publish data use the hub's `publish-data` pattern (direct push, PR fallback)."
    satisfied_by: "workflow review"
    seed: "hub `.github/actions/utilities/publish-data`"
    gap: false
  - id: UPS-AGENT-31
    area: AGENT
    section: "Agent-facing operational rules"
    level: MUST
    applies: [all]
    requirement: "A PR that must have CI is opened with `FLEET_TOKEN`, never `GITHUB_TOKEN`; the token is probed (`gh api user`) not presence-checked."
    satisfied_by: "workflow review"
    seed: "hub `resolve-fleet-token` action"
    gap: false
  - id: UPS-AGENT-32
    area: AGENT
    section: "Agent-facing operational rules"
    level: MUST
    applies: [all]
    requirement: "Issue state for automation lives in labels from the fleet taxonomy (`agent:*`, type, `P0–P3`, `size:*`), never in a database or an event chain; `agent:hold` and `human-review` are the human brakes and are honoured everywhere."
    satisfied_by: "labels exist in repo (`gh label list`)"
    seed: "`templates/community/labels.yml` (gap)"
    gap: true
  - id: UPS-AGENT-33
    area: AGENT
    section: "Agent-facing operational rules"
    level: SHOULD
    applies: [all]
    requirement: "The repo's README or `CLAUDE.md` names the loops that act on it (fleet-pulse doctor, issue pipeline, repo-evolution opt-in) so an agent knows what else is running."
    satisfied_by: "Fleet context section"
    seed: "kit"
    gap: false
  - id: UPS-QA-01
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    requirement: "`.editorconfig` is the hub copy (REPO-17). Formatting is enforced by a formatter, not by review: **Prettier** for JS/TS/JSON/YAML/MD (hub `.prettierrc`: single quotes, semi, es5 commas, width 100, `proseWrap: never`), **ruff format** for Python, **shfmt** optional for Bash, **rubocop** for Ruby gems."
    satisfied_by: "config file present; `format:check` script or pre-commit hook"
    seed: "`templates/lint/` (gap)"
    gap: true
  - id: UPS-QA-02
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    applies_notes: ["all with Python"]
    requirement: "**ruff** is the only Python linter (`[tool.ruff]` in `pyproject.toml`, line length 120, `select = [\"E\",\"F\",\"I\",\"B\",\"UP\"]`); black/flake8/isort are retired except where the hub scopes them to `projects/README`."
    satisfied_by: "pyproject section"
    seed: "—"
    gap: true
  - id: UPS-QA-03
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    applies_notes: ["all with JS/TS"]
    requirement: "ESLint **flat config** (`eslint.config.js`) with `typescript-eslint` recommended and `eslint-config-prettier` last; no `.eslintrc*`."
    satisfied_by: "file present"
    seed: "—"
    gap: true
  - id: UPS-QA-04
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    applies_notes: ["all with Bash"]
    requirement: "`shellcheck --severity=warning` clean; every script `set -euo pipefail`, quotes expansions, and carries the house header."
    satisfied_by: "CI step"
    seed: "`standard-ci` (Bash detection — gap)"
    gap: true
  - id: UPS-QA-05
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    requirement: "Markdown is one paragraph per line (`markdown-oneline.yml` self-healing gate, vendored `unwrap-prose.py`); markdownlint with the hub's disabled-rule set (MD013/MD033/MD041 off)."
    satisfied_by: "gate present"
    seed: "`tools/fanout.sh --kit prose`"
    gap: false
  - id: UPS-QA-06
    area: QA
    section: "Formatting and linting"
    level: SHOULD
    applies: [all]
    requirement: "`.pre-commit-config.yaml` runs the same checks locally (trailing whitespace, EOF, yaml/json, large files, private keys, markdownlint, shellcheck, the formatter). `rev:` pins are the sanctioned exception to always-latest."
    satisfied_by: "file present"
    seed: "hub `.pre-commit-config.yaml` as reference"
    gap: false
  - id: UPS-QA-07
    area: QA
    section: "Formatting and linting"
    level: MUST
    applies: [all]
    applies_notes: ["all with TS"]
    requirement: "`strict: true`; no `as any`, `@ts-ignore`, `@ts-expect-error` without a linked issue; no `# type: ignore` in Python without a reason."
    satisfied_by: "grep-able"
    seed: "—"
    gap: true
  - id: UPS-QA-10
    area: QA
    section: "Tests"
    level: MUST
    applies: [app, api, lib, cli, ext]
    requirement: "A unit-test runner is configured and **collects at least one test**; the CI gate fails on zero tests for these kinds (today's exit-code-5 pass is retained only for `site`/`content`)."
    satisfied_by: "runner config + ≥1 test"
    seed: "`standard-ci` change (gap)"
    gap: true
  - id: UPS-QA-11
    area: QA
    section: "Tests"
    level: MUST
    applies: [all]
    applies_notes: ["all with Python"]
    requirement: "`pytest`, configured in `[tool.pytest.ini_options]` of `pyproject.toml` (not `pytest.ini`/`setup.cfg`), tests in `tests/`, AAA pattern, `pytest-cov` reporting."
    satisfied_by: "pyproject section"
    seed: "—"
    gap: true
  - id: UPS-QA-12
    area: QA
    section: "Tests"
    level: MUST
    applies: [all]
    applies_notes: ["all with JS/TS (non-ext)"]
    requirement: "**vitest** + Testing Library for units; **Playwright** for e2e and visual. Cypress is retired (migrate `cv-builder-pro`). Tests colocated as `*.test.ts(x)` or under `tests/`."
    satisfied_by: "`vitest.config.ts`, `playwright.config.ts`"
    seed: "—"
    gap: true
  - id: UPS-QA-13
    area: QA
    section: "Tests"
    level: MUST
    applies: [ext]
    requirement: "VS Code extensions: **esbuild** bundler, **vitest** for unit tests, `@vscode/test-electron` for integration; one shared `extension.config` shape (lift from `vs-sonic-pi`)."
    satisfied_by: "files present"
    seed: "`templates/vscode-extension/` (gap)"
    gap: true
  - id: UPS-QA-14
    area: QA
    section: "Tests"
    level: SHOULD
    applies: [app, api, lib]
    requirement: "Coverage floor **70 %** lines on changed files, reported in CI; not a hard gate until the fleet audit sets per-repo baselines."
    satisfied_by: "coverage report artifact"
    seed: "—"
    gap: true
  - id: UPS-QA-15
    area: QA
    section: "Tests"
    level: MUST
    applies: [site]
    requirement: "Jekyll sites run `jekyll build --strict_front_matter` plus `htmlproofer` (internal links, images) in CI; theme repos additionally run the Playwright visual suite."
    satisfied_by: "CI step"
    seed: "`standard-ci` (Ruby branch)"
    gap: false
  - id: UPS-QA-16
    area: QA
    section: "Tests"
    level: SHOULD
    applies: [app, site]
    requirement: "The UX audit gate (FE-60) runs in CI for every UI surface."
    satisfied_by: "CI step"
    seed: "`templates/ux-audit/` (gap)"
    gap: true
  - id: UPS-QA-17
    area: QA
    section: "Tests"
    level: MUST
    applies: [api]
    requirement: "Contract tests: the OpenAPI document is generated in CI and diffed against the committed one; a breaking diff fails without a version bump (BE-30)."
    satisfied_by: "CI step"
    seed: "—"
    gap: true
  - id: UPS-QA-20
    area: QA
    section: "CI"
    level: MUST
    applies: [all]
    applies_notes: ["all except fork"]
    requirement: "`ci.yml` is a thin caller of a reusable workflow: `standard-ci.yml@main` (experiment/content tiers) or `bamr87/.github` `ci.yml@main` (active tier, six jobs incl. CodeQL). Bespoke `ci.yml` is a deviation with a reason."
    satisfied_by: "byte-identical to kit, or stamped"
    seed: "`tools/fanout.sh --artifacts ci`"
    gap: false
  - id: UPS-QA-21
    area: QA
    section: "CI"
    level: MUST
    applies: [all]
    requirement: "Toolchain versions resolve caller input → repo `vars.*` → fleet default (`_data/fleet.yml` `toolchain:`). No per-repo version pins in workflows."
    satisfied_by: "grep"
    seed: "`dash config sync`"
    gap: false
  - id: UPS-QA-22
    area: QA
    section: "CI"
    level: MUST
    applies: [all]
    requirement: "Actions ride major tags (`@vN`); `permissions:` is declared and minimal; `timeout-minutes` set; `concurrency` cancels superseded runs on PRs; no workflow ends in a bare `git push` to a protected branch; `needs:` over `workflow_run`; `actionlint` clean."
    satisfied_by: "hub workflow standards"
    seed: "hub `.github/workflows/README.md`"
    gap: false
  - id: UPS-QA-23
    area: QA
    section: "CI"
    level: MUST
    applies: [all]
    applies_notes: ["all with `action.yml`"]
    requirement: "Composite action manifests contain no `${{ }}` in `description:` prose (drift check (l))."
    satisfied_by: "check"
    seed: "—"
    gap: true
  - id: UPS-QA-24
    area: QA
    section: "CI"
    level: SHOULD
    applies: [all]
    requirement: "Branch protection on the default branch requires the CI gate."
    satisfied_by: "`tools/protect-branch.sh`"
    seed: "hub tool"
    gap: false
  - id: UPS-QA-30
    area: QA
    section: "Commits, branches, releases"
    level: MUST
    applies: [all]
    requirement: "Conventional Commits `type(scope): description`; types `feat fix docs style refactor test chore perf ci build`. Bot commits use the fleet bot identity and the noreply email."
    satisfied_by: "commit history"
    seed: "—"
    gap: true
  - id: UPS-QA-31
    area: QA
    section: "Commits, branches, releases"
    level: MUST
    applies: [all]
    requirement: "Default branch is `main`; work branches `feature/ fix/ docs/ refactor/ test/ chore/`; automation branches use their declared prefixes (`agent/issue-*`, `ai-evolution/*`, `chore/standardize-baseline`)."
    satisfied_by: "branch names"
    seed: "—"
    gap: true
  - id: UPS-QA-32
    area: QA
    section: "Commits, branches, releases"
    level: MUST
    applies: [app, api, lib, cli, ext]
    requirement: "**release-please** manages versions and `CHANGELOG.md` (`release-type` per ecosystem, `simple` for the rest); `release.yml` calls the shared publish workflow; registry `release:` block filled. Kit gains `VERSION` + `archive/` like every other kit."
    satisfied_by: "files present"
    seed: "`tools/adopt-release.sh`"
    gap: false
  - id: UPS-QA-33
    area: QA
    section: "Commits, branches, releases"
    level: MUST
    applies: [site, content]
    requirement: "Sites and content repos still tag releases via release-please `simple` so the CHANGELOG exists; publishing is Pages, not a registry."
    satisfied_by: "config present"
    seed: "`tools/adopt-release.sh`"
    gap: false
  - id: UPS-QA-34
    area: QA
    section: "Commits, branches, releases"
    level: MUST
    applies: [all]
    requirement: "Submodule rule: commit and push in the project repo first; the hub bumps the pointer. Never bundle several submodules in one PR."
    satisfied_by: "`SUBMODULES.md`"
    seed: "—"
    gap: true
  - id: UPS-QA-40
    area: QA
    section: "Dependencies"
    level: MUST
    applies: [all]
    requirement: "Always-latest: no exact pins, no ceilings, no committed lockfiles; floors (`>=`) are fine. Exceptions: Actions `@vN`, pre-commit `rev:`, fleet toolchain versions."
    satisfied_by: "drift checks (j)/(k)"
    seed: "`tools/unpin-deps.sh`, `deps-fanout.yml`"
    gap: false
  - id: UPS-QA-41
    area: QA
    section: "Dependencies"
    level: MUST
    applies: [all]
    requirement: "Dependabot for `github-actions` weekly, grouped, `ci` prefix; no package-ecosystem entries (always-latest makes them redundant)."
    satisfied_by: "`.github/dependabot.yml`"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-QA-42
    area: QA
    section: "Dependencies"
    level: SHOULD
    applies: [app, api]
    requirement: "A `supply-chain` CI step runs the ecosystem audit (`npm audit --audit-level=high`, `pip-audit`, `bundle audit`) as advisory; findings flow to the fleet-pulse doctor."
    satisfied_by: "CI step"
    seed: "`standard-ci` change (gap)"
    gap: true
  - id: UPS-FE-01
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "One token file defines every design value as CSS custom properties under the fleet prefix **`--fleet-*`** (Jekyll keeps `--zer0-*` as an alias layer): `color`, `space`, `type`, `radius`, `shadow`, `motion`, `layer`, `breakpoint`. Colour literals appear **nowhere else** — enforced by a test (lift `gitorio/app/src/theme.test.ts`) or a CI grep (lift zer0-cms `tokens.css` rule)."
    satisfied_by: "token file + test"
    seed: "`templates/design-tokens/` (gap; source `zer0-mistakes/_design-system/tokens/*.css`)"
    gap: true
  - id: UPS-FE-02
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "Semantic colour roles, not palette names: `primary secondary accent success info warning danger bg bg-elevated bg-muted ink ink-muted border link link-hover code-bg code-ink`. Skins re-map roles; components consume roles."
    satisfied_by: "token names"
    seed: "same"
    gap: false
  - id: UPS-FE-03
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "Spacing scale `--fleet-space-0..5` = 0/.25/.5/1/1.5/3 rem plus `section` and `container-x` fluid tokens; radius `sm base lg xl pill circle`; shadow `xs sm md lg focus`; motion `fast 120ms / base 200ms / slow 320ms` with `cubic-bezier(0.2,0,0,1)`, collapsing to `0.01ms` under `prefers-reduced-motion`."
    satisfied_by: "token values"
    seed: "same"
    gap: false
  - id: UPS-FE-04
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "A **named z-index scale** (`--fleet-layer-sticky 1020 · header 1030 · backdrop 1040 · offcanvas 1045 · fab 1050–1060 · modal 1055 · popover 1070 · tooltip 1080 · toast 1090 · consent 1095 · feedback-modal 1096 · skip-link 1100`). Hand-written `z-index` numbers fail the UX gate."
    satisfied_by: "tokens + gate rule R4"
    seed: "same"
    gap: false
  - id: UPS-FE-05
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "Typography: system font stacks by default (`system-ui` sans, `ui-monospace, SFMono-Regular, Menlo` mono); no webfont without a documented reason; fluid `clamp()` headings; weights 400/500/600/700; line heights 1.2/1.55/1.75."
    satisfied_by: "tokens"
    seed: "same"
    gap: false
  - id: UPS-FE-06
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app]
    requirement: "Colour modes `light`, `dark`, `auto` via `data-theme` (Bootstrap sites: `data-bs-theme`) on `<html>`, persisted in `localStorage`, defaulting to `prefers-color-scheme`, set by an inline pre-paint script so there is no flash. Brand-locked surfaces may set `color_mode_lock`. VS Code webviews derive from `--vscode-*` through the token file only."
    satisfied_by: "init script + toggle"
    seed: "`templates/design-tokens/color-mode-init.js` (gap)"
    gap: true
  - id: UPS-FE-07
    area: FE
    section: "Design tokens"
    level: SHOULD
    applies: [site, app]
    requirement: "Skins are a token re-map only (`[data-theme-skin]`), each shipping WCAG-AA link colours for both modes; the fleet skin set is the zer0-mistakes seven (`air aqua dirt neon mint plum sunrise`)."
    satisfied_by: "skin file"
    seed: "zer0-mistakes `_sass/theme/_skins.scss`"
    gap: false
  - id: UPS-FE-08
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app]
    requirement: "Bootstrap 5.3 (Jekyll) or Tailwind 4 (React/Next) are the two sanctioned utility frameworks; either consumes the token file (`@theme` maps to `--fleet-*`). A repo does not load two frameworks."
    satisfied_by: "one framework"
    seed: "—"
    gap: true
  - id: UPS-FE-09
    area: FE
    section: "Design tokens"
    level: MUST
    applies: [site, app, ext]
    requirement: "The token file has a portable plain-CSS twin checked for drift in CI when the source is SCSS/Tailwind (lift `_design-system/SYNC.md` + `design-system-check.rb`)."
    satisfied_by: "sync check"
    seed: "zer0-mistakes"
    gap: false
  - id: UPS-FE-10
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "AppShell"
    requirement: "Header/topbar with branding and primary nav, exactly one `<main id=\"main-content\">`, footer, mobile drawer; labelled landmarks (`<nav aria-label>`, `<aside aria-label>`); optional left nav / right TOC columns."
    seed: "zer0 `_layouts/root.html`; law-ai `components/shell/AppShell.tsx`; fredgar `layout/AppShell.tsx`"
    gap: false
  - id: UPS-FE-11
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Skip link"
    requirement: "First focusable element, `href=\"#main-content\"`, visible on focus, on the skip-link layer."
    seed: "zer0 `core/header.html`; law-ai AppShell"
    gap: false
  - id: UPS-FE-12
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Theme toggle"
    requirement: "Light/dark/auto segmented control in the header or settings panel; keyboard operable; announces state."
    seed: "zer0 `components/halfmoon.html`; fredgar ThemeToggle"
    gap: false
  - id: UPS-FE-13
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Feedback widget"
    requirement: "The universal \"Improve this page / Report a problem\" FAB + dialog that files a prefilled GitHub issue (full contract in [FEEDBACK.md](FEEDBACK.md))."
    seed: "`templates/feedback/`"
    gap: false
  - id: UPS-FE-14
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Edit / source links"
    requirement: "On every content page: **Edit on GitHub** (`blob/<branch>/<path>`), **Copy link**, and the feedback trigger, as real anchors that work without JS."
    seed: "zer0 `content/intro.html`; cv `builder/html.ts` `editUrl`"
    gap: false
  - id: UPS-FE-15
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "404 page"
    requirement: "A real not-found route/page with search or nav suggestions and a \"report this URL\" link that opens the feedback widget pre-typed as `fix-page`. No `*`→`/` redirects."
    seed: "zer0 `_layouts/404.html`; law-ai `not-found.tsx`; fredgar `NotFound.tsx`"
    gap: false
  - id: UPS-FE-16
    area: FE
    section: "Core component set"
    level: MUST
    applies: [app]
    component: "Error boundary + route states"
    requirement: "Recover-in-place fallback (Try again · Go home · Report via feedback widget with the error attached); `loading`/`error`/`not-found` states per route."
    seed: "fredgar `ErrorBoundary.tsx`; law-ai `app/(app)/{loading,error}.tsx`"
    gap: false
  - id: UPS-FE-17
    area: FE
    section: "Core component set"
    level: MUST
    applies: [app]
    component: "State set"
    requirement: "`Loading`, `Skeleton`, `EmptyState`, `ErrorState`, and a `Query<T>` wrapper that maps one request to all four."
    seed: "fredgar `components/ui/states.tsx`"
    gap: false
  - id: UPS-FE-18
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Toast / announcer"
    requirement: "One toast system: `role=\"status\" aria-live=\"polite\"`, variants success/error/info, keyboard-dismissable, timeout respects reduced motion. Replaces `alert()`."
    seed: "fredgar `lib/toast.tsx`; zer0 `ui-helpers.js`; cv-builder-pro `sonner` (acceptable)"
    gap: false
  - id: UPS-FE-19
    area: FE
    section: "Core component set"
    level: MUST
    applies: [app]
    component: "Form field"
    requirement: "`Field(label, hint, error, required)` primitive with `aria-describedby` wiring; validation via `react-hook-form` + `zod` in React, the framework's forms in Django/Rails. Errors are inline and announced."
    seed: "aieo `ui/Field.tsx` + cv-builder-pro `ui/form.tsx`"
    gap: false
  - id: UPS-FE-20
    area: FE
    section: "Core component set"
    level: MUST
    applies: [app]
    component: "HTTP client"
    requirement: "One client module with one `ApiError` shape (BE-40); feature code never calls `fetch` directly."
    seed: "fredgar `lib/http.ts` + aieo `services/api.ts` merged"
    gap: false
  - id: UPS-FE-21
    area: FE
    section: "Core component set"
    level: SHOULD
    applies: [site, app]
    component: "Search"
    requirement: "`⌘/Ctrl-K` and `/` open a search modal over a static index (`/search.json`) or the app's search API; results keyboard navigable."
    seed: "zer0 `components/search-modal.html`; fredgar ⌘K"
    gap: false
  - id: UPS-FE-22
    area: FE
    section: "Core component set"
    level: SHOULD
    applies: [site, app]
    component: "Keyboard shortcuts + help"
    requirement: "`?` opens a shortcuts modal; shortcuts documented in one place; never override browser defaults."
    seed: "zer0 `modules/navigation/keyboard.js`"
    gap: false
  - id: UPS-FE-23
    area: FE
    section: "Core component set"
    level: SHOULD
    applies: [site]
    component: "TOC, breadcrumbs, back-to-top"
    requirement: "Right-column TOC with scroll-spy on long pages; `BreadcrumbList` microdata breadcrumbs; back-to-top FAB."
    seed: "zer0 `content/toc.html`, `navigation/breadcrumbs.html`, `back-to-top.js`"
    gap: false
  - id: UPS-FE-24
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Consent + analytics wrapper"
    requirement: "No tracker fires before consent; categories essential/analytics/marketing; 365-day persistence; DNT/GPC honoured; production-only gate. Fleet default analytics is **PostHog** (privacy config) with GA4 optional."
    seed: "zer0 `components/cookie-consent.html`, `analytics/posthog.html`"
    gap: false
  - id: UPS-FE-25
    area: FE
    section: "Core component set"
    level: MUST
    applies: [site, app]
    component: "Head/meta block"
    requirement: "`<title>`, `description`, canonical, `og:title/description/image/url/type`, `twitter:card`, favicon set + `theme-color`, `manifest` for apps. Jekyll via `jekyll-seo-tag` + `content/seo.html`; React/Next via a `Head`/`Metadata` helper; Django via `{% block meta %}`."
    seed: "zer0 `core/head.html`; barodybroject `base.html`"
    gap: false
  - id: UPS-FE-26
    area: FE
    section: "Core component set"
    level: SHOULD
    applies: [site, app]
    component: "Comments"
    requirement: "giscus (GitHub Discussions) on article-type pages, theme-following, opt-in per page (`comments: true`)."
    seed: "zer0 `content/giscus.html`"
    gap: false
  - id: UPS-FE-27
    area: FE
    section: "Core component set"
    level: SHOULD
    applies: [site, app]
    component: "Copy affordances"
    requirement: "Copy button on every code block and table (CSV), with the toast announcer."
    seed: "zer0 `code-copy.js`, `table-copy.js`"
    gap: false
  - id: UPS-FE-28
    area: FE
    section: "Core component set"
    level: MAY
    applies: [site, app]
    component: "Settings panel"
    requirement: "An offcanvas with Appearance (mode, primary colour), Site, Developer tabs; runtime overrides persisted to `localStorage`."
    seed: "zer0 `components/info-section.html`"
    gap: false
  - id: UPS-FE-29
    area: FE
    section: "Core component set"
    level: MAY
    applies: [app]
    component: "PWA"
    requirement: "`manifest.webmanifest` + minimal service worker for installable apps."
    seed: "zer0-image-generator `app/views/pwa/`"
    gap: false
  - id: UPS-FE-30
    area: FE
    section: "Component contract"
    level: MUST
    applies: [site, app, ext]
    requirement: "Every component has a doc header (purpose, path, parameters with types and defaults, returns, example); parameter defaults are assigned at the top; filenames kebab-case (`PascalCase.tsx` in React); params snake_case (Liquid) / camelCase (TS); theme-owned classes prefixed `fleet-<component>` (`zer0-` alias in the theme)."
    satisfied_by: "header present"
    seed: "zer0 `_includes/components/README.md`"
    gap: false
  - id: UPS-FE-31
    area: FE
    section: "Component contract"
    level: MUST
    applies: [site, app, ext]
    requirement: "Every component satisfies at least one of: renders a semantic landmark; exposes `aria-label`/`aria-labelledby`/`aria-describedby`; inherits from a documented parent landmark. Icon-only buttons have a visible label or `aria-label`; decorative icons `aria-hidden=\"true\"`."
    satisfied_by: "review + gate"
    seed: "same"
    gap: false
  - id: UPS-FE-32
    area: FE
    section: "Component contract"
    level: MUST
    applies: [site, app, ext]
    requirement: "Components consume tokens, never raw values; a missing token is proposed in the token file, not hardcoded."
    satisfied_by: "grep"
    seed: "same"
    gap: false
  - id: UPS-FE-33
    area: FE
    section: "Component contract"
    level: SHOULD
    applies: [site, app]
    requirement: "A component catalog page renders every component with its variants (`component-showcase` / `_design-system/**/*.card.html` pattern; Storybook is acceptable in React apps but not required)."
    satisfied_by: "catalog route"
    seed: "zer0 `components/component-showcase.html`"
    gap: false
  - id: UPS-FE-34
    area: FE
    section: "Component contract"
    level: SHOULD
    applies: [site, app]
    requirement: "A feature registry (`features/features.yml`: id → layouts/includes/styles/tests) and `Feature: <ID>` markers in component files give traceability from component to test to doc."
    satisfied_by: "registry present"
    seed: "zer0 `features/features.yml`"
    gap: false
  - id: UPS-FE-35
    area: FE
    section: "Component contract"
    level: MUST
    applies: [site]
    requirement: "Consumer sites override the theme only through the four extension hooks (`custom/head.html`, `custom/body-start.html`, `custom/footer.html`, `custom/body-end.html`) and `user-overrides.css/js`; copying theme includes into a consumer is a deviation."
    satisfied_by: "no shadowed theme files"
    seed: "zer0 `_includes/custom/`"
    gap: false
  - id: UPS-FE-40
    area: FE
    section: "Content and front matter (sites)"
    level: MUST
    applies: [site]
    requirement: "Every page carries `title`, `description`, `permalink` (or a collection default), `lastmod`; posts add `date`, `categories`, `tags`, `author`; drafts use `draft:`. Sidebar resolution is page → collection → site."
    satisfied_by: "`--strict_front_matter` build"
    seed: "zer0 `frontmatter.json`"
    gap: false
  - id: UPS-FE-41
    area: FE
    section: "Content and front matter (sites)"
    level: SHOULD
    applies: [site]
    requirement: "Each post has a 4:3 preview image under `assets/images/previews/<slug>.png` used as `og:image`."
    satisfied_by: "file present"
    seed: "zer0 preview generator"
    gap: false
  - id: UPS-FE-42
    area: FE
    section: "Content and front matter (sites)"
    level: MUST
    applies: [site]
    requirement: "`jekyll-seo-tag`, `jekyll-sitemap`, `jekyll-feed`, `jekyll-redirect-from`, `jekyll-include-cache` are enabled; `/feed.xml` and `/sitemap.xml` exist; `aliases:` handles moved URLs."
    satisfied_by: "`_config.yml`"
    seed: "—"
    gap: true
  - id: UPS-FE-43
    area: FE
    section: "Content and front matter (sites)"
    level: SHOULD
    applies: [site]
    requirement: "English under `pages/**` is the only human-maintained language; alternates are generated (`translate.yml`) with `hreflang` alternates and a translation notice."
    satisfied_by: "config"
    seed: "zer0 `translate.yml`"
    gap: false
  - id: UPS-FE-50
    area: FE
    section: "Accessibility and performance"
    level: MUST
    applies: [site, app, ext]
    requirement: "WCAG 2.1 AA: one `:focus-visible` ring token used everywhere; visible focus never removed; contrast ≥ 4.5:1 for text in every mode and skin; `maximumScale` ≥ 5 in the viewport meta; all interactive controls keyboard reachable; modals trap focus and close on Escape."
    satisfied_by: "gate rules R5/R11 + axe"
    seed: "law-ai `ui/base.css`; zer0 `utilities/_focus.scss`"
    gap: false
  - id: UPS-FE-51
    area: FE
    section: "Accessibility and performance"
    level: MUST
    applies: [site, app]
    requirement: "`prefers-reduced-motion` disables non-essential motion (token collapse, FE-03)."
    satisfied_by: "tokens"
    seed: "—"
    gap: true
  - id: UPS-FE-52
    area: FE
    section: "Accessibility and performance"
    level: MUST
    applies: [site, app]
    requirement: "Exactly one scroll container owns the viewport; sticky offsets come from tokens; no horizontal page scroll — wide content scrolls inside its own container."
    satisfied_by: "gate rules R1/R2/R9"
    seed: "law-ai `ux_audit.py`"
    gap: false
  - id: UPS-FE-53
    area: FE
    section: "Accessibility and performance"
    level: SHOULD
    applies: [site, app]
    requirement: "Lighthouse budgets in CI: performance ≥ 90 on the home route, accessibility ≥ 95, no layout shift > 0.1; JS budget 200 KB gzipped for sites, documented per app."
    satisfied_by: "CI step"
    seed: "`templates/ux-audit/` (gap)"
    gap: true
  - id: UPS-FE-54
    area: FE
    section: "Accessibility and performance"
    level: SHOULD
    applies: [site, app]
    requirement: "Images lazy-load with intrinsic dimensions; vendored CSS/JS pinned by a manifest (`vendor-manifest.json`), never two copies of one framework."
    satisfied_by: "review"
    seed: "zer0 `vendor-manifest.json`"
    gap: false
  - id: UPS-FE-60
    area: FE
    section: "The UX audit gate"
    level: SHOULD
    applies: [site, app]
    requirement: "A machine-checked UX audit runs in CI with the fleet rule set: **R1** one scroller · **R2** viewport ownership · **R3** single `<main>` · **R4** z-scale only · **R5** focus indicator · **R6** landmark labels · **R7** page layout primitive · **R8** route states · **R9** sticky offsets · **R10** composite widget names · **R11** control names · **R12** shared-CSS drift · **R13** token literal leak. An `ux-audit: exempt — <reason>` marker is the escape hatch."
    satisfied_by: "script + CI step"
    seed: "`templates/ux-audit/` (gap; source `law-ai/scripts/ux_audit.py`)"
    gap: true
  - id: UPS-FE-61
    area: FE
    section: "The UX audit gate"
    level: SHOULD
    applies: [site, app]
    requirement: "Playwright visual specs cover the AppShell, both colour modes, the feedback dialog, and the 404 page."
    satisfied_by: "specs present"
    seed: "zer0 `test/visual/features/`"
    gap: false
  - id: UPS-FB-01
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "Every page renders the feedback trigger: a floating action button (bottom-right, on the `fab` layer, label \"Improve this page\") **and** an inline trigger in the page action bar (FE-14). Both open the same dialog. Webviews use an inline trigger only."
    satisfied_by: "element present on every route"
    seed: "`templates/feedback/`"
    gap: false
  - id: UPS-FB-02
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "The dialog is a native `<dialog>` (or equivalent with `role=\"dialog\" aria-modal=\"true\"`), focus-trapped, Escape/backdrop closable, returns focus to the trigger, labelled by its heading."
    satisfied_by: "a11y review"
    seed: "kit"
    gap: false
  - id: UPS-FB-03
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "The dialog offers **request types** from a taxonomy (FB-20) as a keyboard-navigable radio group, a description textarea with a type-specific placeholder, and a **context preview** listing exactly what will be attached (page, URL, environment, N log lines) so the user consents to it."
    satisfied_by: "markup"
    seed: "kit"
    gap: false
  - id: UPS-FB-04
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "Console/error capture: a ring buffer (default 40 entries) of `console.warn/error`, `window.onerror`, and `unhandledrejection`, installed as early as possible; attached only when the user leaves \"include logs\" checked; previewable before submit."
    satisfied_by: "`window.__fleetFeedback.logs`"
    seed: "kit (`fleet-feedback.js` installs on load)"
    gap: false
  - id: UPS-FB-05
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "Default mode is **`url`**: open `https://github.com/<repo>/issues/new?title=&body=&labels=&assignees=` in a new tab with no token. Optional **`proxy`** mode POSTs `{title, body, labels}` to a configured endpoint that holds the token server-side."
    satisfied_by: "attribute `mode`"
    seed: "kit"
    gap: false
  - id: UPS-FB-06
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "URL budget **7000 chars**; when exceeded, sections are trimmed in order logs → directive → environment; when still over, or when the popup is blocked, the full body is copied to the clipboard and an `aria-live` status tells the user to paste it. Nothing is silently lost."
    satisfied_by: "code path"
    seed: "kit"
    gap: false
  - id: UPS-FB-07
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "Works without JS for the inline trigger: it is a real `<a href=\"…/issues/new?template=page_feedback.yml&labels=page-feedback\">`, progressively enhanced."
    satisfied_by: "markup"
    seed: "kit + `page_feedback.yml` issue template"
    gap: false
  - id: UPS-FB-08
    area: FB
    section: "Behaviour"
    level: MUST
    applies: [site, app]
    requirement: "Privacy: no cookies, no analytics event without consent, no request until the user submits; the environment section never includes IP, user identity, or form contents; logs are shown before sending."
    satisfied_by: "review"
    seed: "kit"
    gap: false
  - id: UPS-FB-09
    area: FB
    section: "Behaviour"
    level: SHOULD
    applies: [site, app]
    requirement: "Other components route through it: the 404 page opens it pre-typed `fix-page` with the missing URL; the error boundary opens it pre-typed `bug` with the stack attached; \"Suggest an edit\" opens it `improve-page`."
    satisfied_by: "integrations"
    seed: "kit API `open({type, extra})`"
    gap: false
  - id: UPS-FB-10
    area: FB
    section: "Behaviour"
    level: MAY
    applies: [site, app]
    requirement: "An \"Analyze with AI\" step (proxy mode only) sends the draft to a triage endpoint that returns a suggested title, labels, and priority before filing."
    satisfied_by: "config `ai.enabled`"
    seed: "zer0-mistakes chat proxy"
    gap: false
  - id: UPS-FB-20
    area: FB
    section: "Issue contract"
    level: MUST
    applies: [site, app]
    requirement: "Request-type taxonomy (data file `feedback_types.yml`, JSON-injected at runtime): `fix-page`, `improve-page`, `expand-page`, `update-page`, `accessibility` (scope page) · `ui-ux`, `performance`, `feature`, `question` (scope site). Each entry: `id label icon group scope description labels agent directive placeholder`."
    satisfied_by: "data file"
    seed: "`templates/feedback/feedback_types.yml`"
    gap: false
  - id: UPS-FB-21
    area: FB
    section: "Issue contract"
    level: MUST
    applies: [site, app]
    requirement: "Labels applied = marker `page-feedback` + the type's labels, where the type labels map onto the **fleet issue-pipeline taxonomy** (`_data/fleet.yml` `issue_pipeline.labels.types`: `bug feature docs chore ci refactor test security question`) plus optional `area:*`. Every label MUST exist in the repo (GitHub silently drops unknown labels from a prefilled URL)."
    satisfied_by: "`gh label list` ⊇ taxonomy"
    seed: "`templates/community/labels.yml` (gap)"
    gap: true
  - id: UPS-FB-22
    area: FB
    section: "Issue contract"
    level: MUST
    applies: [site, app]
    requirement: "Title: `[<type label>] <page title>` (≤ 240 chars)."
    satisfied_by: "code"
    seed: "kit"
    gap: false
  - id: UPS-FB-23
    area: FB
    section: "Issue contract"
    level: MUST
    applies: [site, app]
    requirement: "Body sections, in order, each a `##` heading: **Description** (user text) · **Page context** table (Page, URL, Source link `blob/<branch>/<path>`, Collection/route, Last modified) · **Environment** table (browser, viewport@dpr, colour scheme + reduced-motion, referrer, repository, branch, build env, captured at) · **Console & error logs** in a `<details>` block · **Agent directive** (type's `directive`, only when `agent: true`) · footer `_Filed from <url> via fleet-feedback v<kit version>._` plus a hidden marker `<!-- fleet-feedback v1 type=<id> -->` for dedupe and analytics."
    satisfied_by: "body"
    seed: "kit"
    gap: false
  - id: UPS-FB-24
    area: FB
    section: "Issue contract"
    level: MUST
    applies: [site, app]
    requirement: "Types with `agent: true` add the configured assignee (default `copilot`; `''` disables) and the directive so the issue is `agent:queued`-eligible on the next pipeline scan; `agent: false` types get neither."
    satisfied_by: "config"
    seed: "kit"
    gap: false
  - id: UPS-FB-25
    area: FB
    section: "Issue contract"
    level: SHOULD
    applies: [site, app]
    requirement: "The repo's `page_feedback.yml` issue template mirrors the same sections so hand-filed and widget-filed issues look identical to the pipeline."
    satisfied_by: "template present"
    seed: "`templates/community/` (gap)"
    gap: true
  - id: UPS-FB-30
    area: FB
    section: "Configuration"
    level: MUST
    applies: [site, app]
    requirement: "Configuration keys (attributes on the element, `page_feedback:` in `_config.yml`, or the React props): `repo` (required, `owner/name`), `branch` (default `main`), `source` (page source path), `labels` (marker, default `page-feedback`), `assignee`, `mode` (`url`/`proxy`), `endpoint`, `capture-logs` (default true), `log-limit` (40), `fab` (default true), `label` (\"Improve this page\"), `types` (URL or inline JSON)."
    satisfied_by: "attributes"
    seed: "kit README"
    gap: false
  - id: UPS-FB-31
    area: FB
    section: "Configuration"
    level: MUST
    applies: [site]
    requirement: "Consumer Jekyll sites enable it in their own `_config.yml` (`page_feedback.enabled: true`, `repository:`); the theme falls back to the kit's built-in taxonomy when `_data/feedback_types.yml` is absent, so `remote_theme` consumers work with zero data files."
    satisfied_by: "config key"
    seed: "theme change (gap) + kit fallback"
    gap: true
  - id: UPS-FB-32
    area: FB
    section: "Configuration"
    level: MUST
    applies: [app]
    requirement: "React/Next apps mount `<FeedbackButton>` once in the AppShell; Django/Rails/MkDocs include the snippet in the base template; VS Code webviews mount the inline variant with `mode=\"proxy\"` (the extension host files the issue via `vscode.env.openExternal`)."
    satisfied_by: "mount point"
    seed: "kit adapters"
    gap: false
  - id: UPS-FB-33
    area: FB
    section: "Configuration"
    level: SHOULD
    applies: [site, app]
    requirement: "The hub dash mounts it on every surface against `bamr87/bamr87`, so a visitor can file against the control plane itself."
    satisfied_by: "element present"
    seed: "hub change"
    gap: false
  - id: UPS-BE-01
    area: BE
    section: "Surface"
    level: MUST
    applies: [api]
    requirement: "Base path `/api/v<N>/`; resources plural kebab-case nouns; ids in the path; filters/sort/pagination in the query string; verbs only for RPC-style actions under `/api/v<N>/<resource>/<id>/<action>`."
    satisfied_by: "route table"
    seed: "—"
    gap: true
  - id: UPS-BE-02
    area: BE
    section: "Surface"
    level: MUST
    applies: [api]
    requirement: "JSON only (`application/json; charset=utf-8`) for request and response bodies; `snake_case` keys; ISO-8601 UTC timestamps with `Z`; ids as strings."
    satisfied_by: "review"
    seed: "—"
    gap: true
  - id: UPS-BE-03
    area: BE
    section: "Surface"
    level: MUST
    applies: [api]
    requirement: "Pagination is cursor-based for lists (`?limit=&cursor=`, response `{items, next_cursor}`); offset pagination only for admin tables with a documented reason."
    satisfied_by: "review"
    seed: "—"
    gap: true
  - id: UPS-BE-04
    area: BE
    section: "Surface"
    level: MUST
    applies: [api]
    requirement: "Every response carries `X-Request-Id` (echoing the inbound one if present) and the app's `X-App-Version`."
    satisfied_by: "headers"
    seed: "middleware (gap: `templates/api/`)"
    gap: true
  - id: UPS-BE-05
    area: BE
    section: "Surface"
    level: SHOULD
    applies: [api]
    requirement: "Rate limiting returns `429` with `Retry-After`; auth failures return `401` (missing) or `403` (insufficient) and never leak whether a resource exists."
    satisfied_by: "tests"
    seed: "—"
    gap: true
  - id: UPS-BE-06
    area: BE
    section: "Surface"
    level: MUST
    applies: [api]
    requirement: "CORS is explicit: an allowlist from config (OPS-01), never `*` with credentials."
    satisfied_by: "config"
    seed: "—"
    gap: true
  - id: UPS-BE-10
    area: BE
    section: "Health and version endpoints"
    level: MUST
    applies: [api, app]
    applies_notes: ["app (server-rendered)"]
    requirement: "`GET /healthz` → `200 {\"status\":\"ok\"}` with no dependencies touched (liveness); `GET /readyz` → `200/503 {\"status\":\"ok\"|\"degraded\",\"checks\":{\"db\":\"ok\",\"cache\":\"ok\",…}}` (readiness). Both unauthenticated, uncached, excluded from analytics and rate limits. The Dockerfile `HEALTHCHECK` and the frontend `ReadyDot` (fredgar) consume `/readyz`."
    satisfied_by: "endpoints"
    seed: "`templates/api/` (gap)"
    gap: true
  - id: UPS-BE-11
    area: BE
    section: "Health and version endpoints"
    level: MUST
    applies: [api, app]
    applies_notes: ["app (server-rendered)"]
    requirement: "`GET /version` → `{\"name\",\"version\",\"commit\",\"built_at\"}` from release-please's version and the build's `GIT_SHA`."
    satisfied_by: "endpoint"
    seed: "same"
    gap: false
  - id: UPS-BE-12
    area: BE
    section: "Health and version endpoints"
    level: SHOULD
    applies: [api]
    requirement: "`GET /api/v<N>/openapi.json` serves the generated OpenAPI 3.1 document; docs UI at `/api/v<N>/docs` in non-production."
    satisfied_by: "route"
    seed: "framework default (FastAPI/DRF spectacular)"
    gap: false
  - id: UPS-BE-20
    area: BE
    section: "Errors"
    level: MUST
    applies: [api]
    requirement: "One error envelope for every non-2xx: `{\"error\":{\"code\":\"<domain>.<reason>\",\"message\":\"<human>\",\"status\":<int>,\"details\":[…],\"request_id\":\"…\",\"retry_after\":<seconds|null>}}`. `code` is a stable, documented string (`auth.invalid_token`, `validation.failed`, `resource.not_found`, `rate.limited`, `upstream.unavailable`); `details` for field errors is `[{\"field\",\"code\",\"message\"}]`."
    satisfied_by: "envelope"
    seed: "`templates/api/` (gap)"
    gap: true
  - id: UPS-BE-21
    area: BE
    section: "Errors"
    level: MUST
    applies: [api]
    requirement: "Unhandled exceptions return the envelope with `code: \"internal.error\"` and a `request_id`, never a stack trace; the trace goes to the log (OPS-10) with the same request id."
    satisfied_by: "handler"
    seed: "same"
    gap: false
  - id: UPS-BE-22
    area: BE
    section: "Errors"
    level: MUST
    applies: [api]
    requirement: "Validation errors are `422` with per-field details (FastAPI/DRF native shapes are adapted into the envelope, not exposed raw)."
    satisfied_by: "handler"
    seed: "same"
    gap: false
  - id: UPS-BE-23
    area: BE
    section: "Errors"
    level: SHOULD
    applies: [api]
    requirement: "Error codes are enumerated in `docs/API-ERRORS.md` (or generated from the enum) and referenced by the frontend's `ApiError` mapping."
    satisfied_by: "doc"
    seed: "—"
    gap: true
  - id: UPS-BE-30
    area: BE
    section: "Versioning and contracts"
    level: MUST
    applies: [api]
    requirement: "Breaking changes bump `v<N>`; the previous version keeps serving for one release cycle with a `Deprecation` header and a `Sunset` date. Additive changes never bump."
    satisfied_by: "changelog + headers"
    seed: "—"
    gap: true
  - id: UPS-BE-31
    area: BE
    section: "Versioning and contracts"
    level: MUST
    applies: [api]
    requirement: "The OpenAPI document is committed (`openapi.json` at the surface root) and CI regenerates and diffs it (QA-17); the frontend's types are generated from it (`openapi-typescript`) rather than hand-written."
    satisfied_by: "CI step + generated types"
    seed: "—"
    gap: true
  - id: UPS-BE-32
    area: BE
    section: "Versioning and contracts"
    level: SHOULD
    applies: [api]
    applies_notes: ["api + app"]
    requirement: "Cross-repo contracts (e.g. the `CVData` schema shared by `cv` and `cv-builder-pro`) are vendored as a JSON Schema under `contract/` with a `contract-check` test on both sides."
    satisfied_by: "`contract/*.schema.json`"
    seed: "cv-builder-pro `scripts/contract-check.mjs`"
    gap: false
  - id: UPS-BE-40
    area: BE
    section: "Client contract (what the frontend HTTP client MUST do)"
    level: MUST
    applies: [app]
    requirement: "One module (`lib/http.ts` / `services/api.ts`) exports `request<T>()`, `get/post/put/patch/del`, and `class ApiError { status; code; message; details; requestId; retryAfter; get isAuth(); get isRateLimit(); get isNetwork() }` parsed from the envelope (BE-20). Feature code never calls `fetch`."
    satisfied_by: "module + lint rule (`no-restricted-globals: fetch` outside it)"
    seed: "`templates/api/client.ts` (gap; merge fredgar + aieo)"
    gap: true
  - id: UPS-BE-41
    area: BE
    section: "Client contract (what the frontend HTTP client MUST do)"
    level: MUST
    applies: [app]
    requirement: "Base URL from one env var (`VITE_API_BASE` / `NEXT_PUBLIC_API_BASE`); credentials from one storage key; `AbortController` timeout (default 30 s, overridable); `X-Request-Id` generated per call and surfaced in error toasts and the feedback widget."
    satisfied_by: "code"
    seed: "same"
    gap: false
  - id: UPS-BE-42
    area: BE
    section: "Client contract (what the frontend HTTP client MUST do)"
    level: SHOULD
    applies: [app]
    requirement: "Server state via TanStack Query (React) with the `Query<T>` state wrapper (FE-17); retries only for `isNetwork` or `5xx`, never for `4xx`."
    satisfied_by: "code"
    seed: "fredgar"
    gap: false
  - id: UPS-BE-43
    area: BE
    section: "Client contract (what the frontend HTTP client MUST do)"
    level: SHOULD
    applies: [app]
    requirement: "A `ReadyDot`-style indicator polls `/readyz` and shows degraded state in the shell."
    satisfied_by: "component"
    seed: "fredgar `AppShell.tsx`"
    gap: false
  - id: UPS-BE-50
    area: BE
    section: "Auth"
    level: MUST
    applies: [api]
    requirement: "Bearer tokens in `Authorization: Bearer …` or an `X-API-Key` header; never in query strings; tokens never logged (OPS-12)."
    satisfied_by: "review"
    seed: "—"
    gap: true
  - id: UPS-BE-51
    area: BE
    section: "Auth"
    level: SHOULD
    applies: [app]
    requirement: "Browser apps needing user identity use Firebase Auth (the fleet's one proven provider, cv-builder-pro) behind a `Protected` route wrapper and an `AuthContext`; server apps use the framework's session auth. New providers need a documented reason."
    satisfied_by: "code"
    seed: "cv-builder-pro `context/AuthContext.tsx`"
    gap: false
  - id: UPS-OPS-01
    area: OPS
    section: "Configuration"
    level: MUST
    applies: [app, api, cli]
    requirement: "Configuration comes from environment variables read **once** at startup into a typed settings object (pydantic-settings, `zod`-parsed `process.env`, Rails credentials); a missing required variable fails fast with the variable's name; defaults are safe for local dev."
    satisfied_by: "settings module"
    seed: "`templates/api/` (gap)"
    gap: true
  - id: UPS-OPS-02
    area: OPS
    section: "Configuration"
    level: MUST
    applies: [app, api, cli]
    requirement: "Env var naming: `SCREAMING_SNAKE`, prefixed by app name for app-specific values (`AIEO_API_KEY`), unprefixed only for fleet-standard ones: `LOG_LEVEL`, `LOG_FORMAT`, `PORT`, `DATABASE_URL`, `REDIS_URL`, `SENTRY_DSN`, `POSTHOG_KEY`, `GIT_SHA`, `APP_ENV` (`development` | `test` | `production`). Browser-exposed vars use the framework prefix (`VITE_`, `NEXT_PUBLIC_`) and are never secrets."
    satisfied_by: "`.env.example` names"
    seed: "—"
    gap: true
  - id: UPS-OPS-03
    area: OPS
    section: "Configuration"
    level: MUST
    applies: [all]
    applies_notes: ["all with env config"]
    requirement: "`.env.example` is complete and committed; `.env*` is gitignored; secrets are never in `_config.yml`, `_data/`, or any committed file (the hub's `_data/` is public site data)."
    satisfied_by: "gitignore + grep"
    seed: "`tools/unpin-deps.sh` gitignore block"
    gap: false
  - id: UPS-OPS-04
    area: OPS
    section: "Configuration"
    level: SHOULD
    applies: [app, api]
    requirement: "Feature flags are env-driven booleans documented in `.env.example`; no flag lives in code longer than one release without a linked issue."
    satisfied_by: "review"
    seed: "—"
    gap: true
  - id: UPS-OPS-10
    area: OPS
    section: "Logging, errors, telemetry"
    level: MUST
    applies: [api, cli]
    requirement: "Structured logs: JSON lines in production (`LOG_FORMAT=json`), human format in dev; fields `ts` (ISO UTC), `level`, `msg`, `logger`, `request_id`, `app`, `version`, plus context; levels `DEBUG INFO WARNING ERROR`; `LOG_LEVEL` env-driven. Python: stdlib `logging` + a JSON formatter; Node: `pino`; Ruby: `Rails.logger` with lograge."
    satisfied_by: "formatter + fields"
    seed: "`templates/api/` (gap)"
    gap: true
  - id: UPS-OPS-11
    area: OPS
    section: "Logging, errors, telemetry"
    level: MUST
    applies: [api]
    requirement: "Every request logs one line at completion: method, path, status, duration ms, `request_id`; health endpoints excluded."
    satisfied_by: "middleware"
    seed: "same"
    gap: false
  - id: UPS-OPS-12
    area: OPS
    section: "Logging, errors, telemetry"
    level: MUST
    applies: [all]
    requirement: "Never log secrets, tokens, `Authorization` headers, or full request bodies; a redaction filter covers the fleet token prefixes (`sk-ant-`, `ghp_`, `github_pat_`, `AIza`)."
    satisfied_by: "filter + test"
    seed: "lift `tools/issue-evidence.sh` scrub list"
    gap: false
  - id: UPS-OPS-13
    area: OPS
    section: "Logging, errors, telemetry"
    level: SHOULD
    applies: [app, api]
    requirement: "Error reporting to Sentry (or compatible) when `SENTRY_DSN` is set, tagged with `version` and `request_id`; the frontend error boundary (FE-16) reports too, and offers the feedback widget as the human path."
    satisfied_by: "init code"
    seed: "—"
    gap: true
  - id: UPS-OPS-14
    area: OPS
    section: "Logging, errors, telemetry"
    level: SHOULD
    applies: [app, api]
    requirement: "Product analytics is PostHog (privacy config, consent-gated per FE-24), server events via `POSTHOG_KEY`; no other tracker without a reason."
    satisfied_by: "init code"
    seed: "zer0 `analytics/posthog.html`"
    gap: false
  - id: UPS-OPS-15
    area: OPS
    section: "Logging, errors, telemetry"
    level: MAY
    applies: [api]
    requirement: "OpenTelemetry traces exported when `OTEL_EXPORTER_OTLP_ENDPOINT` is set; AI calls emit token/cost attributes (FF-0003)."
    satisfied_by: "instrumentation"
    seed: "—"
    gap: true
  - id: UPS-OPS-16
    area: OPS
    section: "Logging, errors, telemetry"
    level: MUST
    applies: [all]
    applies_notes: ["all with AI calls"]
    requirement: "Every Claude/LLM call site is OAuth-first (`CLAUDE_CODE_OAUTH_TOKEN`, `ANTHROPIC_API_KEY` fallback), names its model from config not code, sets `max_tokens`, and logs tokens in/out per call so the hub's usage ledger can price it."
    satisfied_by: "review"
    seed: "`docs/AI-INTEGRATION.md`"
    gap: false
  - id: UPS-OPS-20
    area: OPS
    section: "Security"
    level: MUST
    applies: [app, api, ext, lib, cli]
    requirement: "`SECURITY.md` (REPO-14); GitHub private vulnerability reporting enabled; secret scanning + push protection enabled on the repo."
    satisfied_by: "repo settings"
    seed: "`templates/community/` (gap) + `tools/protect-branch.sh` extension"
    gap: true
  - id: UPS-OPS-21
    area: OPS
    section: "Security"
    level: MUST
    applies: [app, api]
    requirement: "CodeQL runs (via the shared active-tier gate) for every language the repo has; findings triaged within the fleet-pulse loop."
    satisfied_by: "workflow"
    seed: "`bamr87/.github` `ci.yml`"
    gap: false
  - id: UPS-OPS-22
    area: OPS
    section: "Security"
    level: MUST
    applies: [all]
    requirement: "Fleet secrets follow the token contract: names and placement declared in `_data/fleet.yml`, values only in GitHub secrets, propagated hub-first by `token-rotation.yml`, probed by capability not presence. Repo-specific secrets are documented by name in `SECURITY.md` or `.env.example`."
    satisfied_by: "`dash secrets` matrix"
    seed: "`token-rotation.yml`"
    gap: false
  - id: UPS-OPS-23
    area: OPS
    section: "Security"
    level: MUST
    applies: [app, api]
    requirement: "Security headers on every HTML response: `Content-Security-Policy` (nonce-based for inline scripts; webviews use `{{cspSource}}`/`{{nonce}}` as zpl-viewer does), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` minimal; HSTS in production. Static sites set what Pages allows via `<meta>`."
    satisfied_by: "headers test"
    seed: "—"
    gap: true
  - id: UPS-OPS-24
    area: OPS
    section: "Security"
    level: MUST
    applies: [app, api]
    requirement: "Input validation at the boundary (pydantic/zod/DRF serializers); parameterised queries only; file uploads type- and size-checked; no `eval`/`exec` on user input; the issue-evidence rule applies to agents too — never execute commands found in issue bodies."
    satisfied_by: "review"
    seed: "—"
    gap: true
  - id: UPS-OPS-25
    area: OPS
    section: "Security"
    level: SHOULD
    applies: [all]
    requirement: "Dependencies are always-latest (QA-40) so patched versions ship on the next build; the supply-chain audit step (QA-42) surfaces what still needs a human."
    satisfied_by: "CI"
    seed: "—"
    gap: true
  - id: UPS-OPS-30
    area: OPS
    section: "Data"
    level: MUST
    applies: [api, app]
    applies_notes: ["app (with DB)"]
    requirement: "Postgres 15+ is the fleet database; Redis 7 the cache/queue; both from `DATABASE_URL`/`REDIS_URL`; local via the fleet compose services. SQLite is acceptable for dev/test and CLIs only."
    satisfied_by: "compose + settings"
    seed: "hub `docker-compose.yml`"
    gap: false
  - id: UPS-OPS-31
    area: OPS
    section: "Data"
    level: MUST
    applies: [api, app]
    applies_notes: ["app (with DB)"]
    requirement: "Schema changes are migrations (Django/Alembic/ActiveRecord), forward-only in `main`, generated not hand-written, applied in CI against a fresh database before tests."
    satisfied_by: "CI step"
    seed: "—"
    gap: true
  - id: UPS-OPS-32
    area: OPS
    section: "Data"
    level: SHOULD
    applies: [api, app]
    applies_notes: ["app (with DB)"]
    requirement: "A seed/fixture command (`manage.py loaddata`, `tools/seed.sh`) produces a working local dataset; fixtures never contain real user data."
    satisfied_by: "command documented in README"
    seed: "—"
    gap: true
  - id: UPS-OPS-33
    area: OPS
    section: "Data"
    level: MUST
    applies: [all]
    requirement: "Committed data files (`_data/*.yml`, `data/*.json`) are public: no PII, no credentials, and generated ones are marked `generated` in `SCHEMA.md` and regenerated, never hand-edited."
    satisfied_by: "schema lint"
    seed: "—"
    gap: true
  - id: UPS-OPS-34
    area: OPS
    section: "Data"
    level: SHOULD
    applies: [api, app]
    requirement: "Backups and retention are documented in `docs/OPERATIONS.md` for any persistent store the product owns; a restore has been exercised once."
    satisfied_by: "doc"
    seed: "—"
    gap: true
  - id: UPS-OPS-40
    area: OPS
    section: "Deployment"
    level: MUST
    applies: [site]
    requirement: "GitHub Pages via the shared deploy pattern (`actions/deploy-pages`), one Pages surface per repo, `baseurl` correct, custom domain in `CNAME` when used."
    satisfied_by: "workflow"
    seed: "`standard-ci` / `jekyll-gh-pages`"
    gap: false
  - id: UPS-OPS-41
    area: OPS
    section: "Deployment"
    level: SHOULD
    applies: [app, api]
    requirement: "Deploy from a tagged release only (release-please tag → `publish.yml`); environments `staging`/`production` as GitHub Environments with required reviewers on production; rollback is redeploying the previous tag."
    satisfied_by: "workflow"
    seed: "`templates/release-pipeline/`"
    gap: false
  - id: UPS-OPS-42
    area: OPS
    section: "Deployment"
    level: MUST
    applies: [app, api]
    requirement: "The running build exposes its version (BE-11) and the deploy workflow verifies `/readyz` before marking success."
    satisfied_by: "workflow step"
    seed: "—"
    gap: true
