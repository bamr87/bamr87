# Fleet features index lists every repo's features with coverage — evidence

> **Feature:** `HUB-007` · **Scenario:** `verify/scenarios/features.yml` · **Result:** **PASS** (2/2 viewports, 11 steps each) · **Generated by:** `tools/dash verify --only features` against `http://127.0.0.1:4000` on 2026-09-05 · **Verified by:** agent (Claude Code, local run — `verified:` stamped in `features/features.yml` when passing)

The /features/ page renders the totals, the attention list, and one section per repo with its features, their docs/tests/scenarios/evidence links, and the verified stamp; the hub's own entries are present.

Every step passed at every viewport: the route answered 200, the `main` landmark is visible, the page does not scroll sideways, and the expected content rendered. What this proves is that the page renders and is usable at desktop and mobile widths from the committed data files — not that the numbers on it are right (that is the generator tests' job).

## What each image shows

| Image | Viewport | What to look at |
| --- | --- | --- |
| `desktop-features.png` | 1280×800 | the page as a user sees it after `/features/` loads |
| `desktop-features-repo.png` | 1280×800 | the same page after following the in-page repo link (anchor navigation) |
| `mobile-features.png` | 390×844 | the page as a user sees it after `/features/` loads |
| `mobile-features-repo.png` | 390×844 | the same page after following the in-page repo link (anchor navigation) |

## Metrics

Raw data: [`report.json`](report.json) — every step's status, timings, console errors (the `ignore_console` list filters third-party network noise), and the final URL per viewport.

## Not covered

Chromium only; no colour-mode toggle; no keyboard-only pass; data is whatever the committed `_data/*.yml` snapshots held on the run date. The Playwright MCP pass (`verify` PR label) is where an agent explores beyond these steps.
