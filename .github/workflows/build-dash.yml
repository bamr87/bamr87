name: 🛰️ Build & Deploy Dash

# Builds the root Jekyll site (zer0-mistakes remote theme), regenerating live
# monitoring data at build time so the Monitor board stays fresh without commits.
# REPLACES build-docs.yml (MkDocs) as the published GitHub Pages surface.

on:
  push:
    branches: [main]
    paths:
      - 'pages/**'
      - '_data/**'
      - '_config.yml'
      - 'index.md'
      - 'assets/**'
      - 'README.md'
      - '.github/scripts/dash-gen/**'
      - '.github/actions/setup/dash-gen/**'
      - '.github/workflows/build-dash.yml'
  # Build (never deploy) on PRs touching the same paths. Without this the site
  # build was verified only AFTER merge, so a broken page reached main before
  # anything noticed — the same shape as the failure that froze the fleet's data
  # for three weeks. The drift gate covers the registry, not Liquid or the theme.
  #
  # The list is duplicated rather than shared via a YAML anchor: anchors are
  # valid YAML and actionlint accepts them, but GitHub Actions' own workflow
  # parser does not support them, so the anchor version parses clean locally and
  # fails on GitHub. Keep the two lists in sync by hand.
  pull_request:
    paths:
      - 'pages/**'
      - '_data/**'
      - '_config.yml'
      - 'index.md'
      - 'assets/**'
      - 'README.md'
      - '.github/scripts/dash-gen/**'
      - '.github/actions/setup/dash-gen/**'
      - '.github/workflows/build-dash.yml'
  schedule:
    # Daily, AFTER the data-refresh jobs have landed (refresh-dash 04:00,
    # reconcile-registry 05:00, fleet-pulse 06:00). Those commits use
    # GITHUB_TOKEN so they can't trigger this workflow themselves — this run is
    # what republishes them. It was every 6h, but three of those four daily
    # builds landed before any new data existed and only rebuilt an unchanged
    # site; the Monitor board's live signals are regenerated here at build time
    # either way.
    - cron: '0 7 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/setup/dash-gen

      - name: Generate monitoring data (ephemeral)
        run: python .github/scripts/dash-gen/dash_gen.py health || echo "health gen degraded; continuing"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Build site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

  deploy:
    needs: build
    # PRs verify the build and stop there. Deploying from a PR would publish
    # unreviewed content to the live dash.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
