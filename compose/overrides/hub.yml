# GENERATED by tools/fleet-compose.py from _data/ports.yml — do not hand-edit.
# devenv's FLEET-mode port map: exactly the allocations hosted inside
# devenv, replacing the broad ranges the hub's own compose publishes.
#
# A server started with `docker compose exec devenv` must bind the same
# number inside the container as the one published here, so every entry
# is `N:N` — that is what the launch.json devenv configs pass as --port.

services:
  devenv:
    ports: !override
      - "127.0.0.1:${JEKYLL_PORT:-4000}:4000"
      - "127.0.0.1:${VITE_HMR_PORT:-5173}:5173"
      - "127.0.0.1:${MKDOCS_PORT:-8000}:8000"
      - "127.0.0.1:${CV_PORT:-5000}:5000"   # cv-builder-pro
      - "127.0.0.1:${BAMR87_IO_PORT:-4012}:4012"   # bamr87.github.io
      - "127.0.0.1:${BAMR87_IO_LIVERELOAD_PORT:-35732}:35732"   # bamr87.github.io livereload
      - "127.0.0.1:${LIFEHACKER_PORT:-4013}:4013"   # lifehacker.dev
      - "127.0.0.1:${LIFEHACKER_LIVERELOAD_PORT:-35733}:35733"   # lifehacker.dev livereload
      - "127.0.0.1:${WARGAMES_PORT:-4015}:4015"   # wargames
      - "127.0.0.1:${WARGAMES_LIVERELOAD_PORT:-35735}:35735"   # wargames livereload
      - "127.0.0.1:${DRSAI_PORT:-4016}:4016"   # drsai
      - "127.0.0.1:${DRSAI_LIVERELOAD_PORT:-35736}:35736"   # drsai livereload
      - "127.0.0.1:${IRONY_WORKS_PORT:-4017}:4017"   # irony-works
      - "127.0.0.1:${IRONY_WORKS_LIVERELOAD_PORT:-35737}:35737"   # irony-works livereload
      - "127.0.0.1:${Y2005_PORT:-4018}:4018"   # 2005
      - "127.0.0.1:${Y2005_LIVERELOAD_PORT:-35738}:35738"   # 2005 livereload
      - "127.0.0.1:${REWIND_ARCADE_PORT:-5175}:5175"   # rewind-arcade
      - "127.0.0.1:${AIEO_FRONTEND_PORT:-5176}:5176"   # aieo-frontend
      - "127.0.0.1:${GITNEXUS_PORT:-5177}:5177"   # gitnexus
      - "127.0.0.1:${AI_SEED_MKDOCS_PORT:-8003}:8003"   # ai-seed-docs
