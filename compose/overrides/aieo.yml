# GENERATED by tools/fleet-compose.py from _data/ports.yml — do not hand-edit.
# Hub-side overrides for projects/aieo — the submodule's own
# docker-compose.yml is untouched and still runs standalone.
#
# `!override` replaces the ports list rather than appending to it, which
# is what lets a hardcoded `8000:8000` become a fleet allocation with no
# upstream commit. `networks:` stays additive so the project keeps its own.

services:
  postgres:
    container_name: fleet-aieo-postgres
    ports: !override
      - "127.0.0.1:${AIEO_DB_PORT:-5443}:5432"
  redis:
    container_name: fleet-aieo-redis
    ports: !override
      - "127.0.0.1:${AIEO_REDIS_PORT:-6392}:6379"
  mcp-server:
    container_name: fleet-aieo-mcp-server
  mcp-audit:
    container_name: fleet-aieo-mcp-audit
  qdrant:
    # bound to loopback (was published on every interface)
    container_name: fleet-aieo-qdrant
    ports: !override
      - "127.0.0.1:6333:6333"
      - "127.0.0.1:6334:6334"
