# GENERATED by tools/fleet-compose.py from _data/ports.yml — do not hand-edit.
# Hub-side overrides for projects/fredgar-ai — the submodule's own
# docker-compose.yml is untouched and still runs standalone.
#
# `!override` replaces the ports list rather than appending to it, which
# is what lets a hardcoded `8000:8000` become a fleet allocation with no
# upstream commit. `networks:` stays additive so the project keeps its own.

services:
  web:
    # debugpy attach point (see .vscode/launch.json)
    # environment: URLs repointed at the remapped host port(s)
    environment:
      CORS_ALLOWED_ORIGINS: "${CORS_ALLOWED_ORIGINS:-http://localhost:${FREDGAR_FRONTEND_PORT:-5111},http://127.0.0.1:${FREDGAR_FRONTEND_PORT:-5111},http://localhost:5174,http://127.0.0.1:5174,http://localhost:5175,http://127.0.0.1:5175,http://localhost:${FREDGAR_FRONTEND_PROD_PORT:-5115},http://127.0.0.1:${FREDGAR_FRONTEND_PROD_PORT:-5115}}"
    ports: !override
      - "127.0.0.1:${FREDGAR_API_PORT:-8112}:8000"
      - "127.0.0.1:${FREDGAR_DEBUG_PORT:-5712}:5678"
  frontend:
    ports: !override
      - "127.0.0.1:${FREDGAR_FRONTEND_PORT:-5111}:5173"
  frontend-prod:
    ports: !override
      - "127.0.0.1:${FREDGAR_FRONTEND_PROD_PORT:-5115}:80"
  tika:
    # bound to loopback (was published on every interface)
    ports: !override
      - "127.0.0.1:9998:9998"
