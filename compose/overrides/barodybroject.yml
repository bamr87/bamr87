# GENERATED by tools/fleet-compose.py from _data/ports.yml — do not hand-edit.
# Hub-side overrides for projects/barodybroject — the submodule's own
# docker-compose.yml is untouched and still runs standalone.
#
# `!override` replaces the ports list rather than appending to it, which
# is what lets a hardcoded `8000:8000` become a fleet allocation with no
# upstream commit. `networks:` stays additive so the project keeps its own.

services:
  web-prod:
    # excluded from the fleet stack: production settings; needs a real SECRET_KEY (dev stack is in .devcontainer/)
    container_name: fleet-barodybroject-web-prod
    profiles: [standalone]
  jekyll:
    container_name: fleet-barodybroject-jekyll
    ports: !override
      - "127.0.0.1:${BARODY_JEKYLL_PORT:-4021}:4002"
  docs:
    container_name: fleet-barodybroject-docs
    ports: !override
      - "127.0.0.1:${BARODY_DOCS_PORT:-8121}:80"
  barodydb:
    container_name: fleet-barodybroject-barodydb
    ports: !override
      - "127.0.0.1:${BARODY_DB_PORT:-5444}:5432"
